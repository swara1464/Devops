#Install venv (system-wide)
sudo apt update
sudo apt install python3.10-venv

#Verify Python version
python3 --version

#Create virtual environment
python3 -m venv venv

#Activate the virtual environment
source venv/bin/activate

#Create requirements file
numpy==1.26.2
pandas==2.2.2
scikit-learn==1.3.2
jupyterlab==4.1.0

#Install ML packages
pip install -r requirements-ml.txt

#Verify installation
pip freeze | grep -E "numpy|pandas|scikit-learn|jupyter"

#Launch JupyterLab
jupyter lab

#Create notebook: ml_setup.ipynb

Cell 1 — Markdown
# ML Environment Setup
This notebook verifies required packages and runs a tiny ML sample.

Cell 2 — Code
import numpy as np
import pandas as pd
from sklearn.linear_model import LinearRegression

print("numpy:", np.__version__)
print("pandas:", pd.__version__)

# tiny regression sample
X = np.array([[1],[2],[3],[4]])
y = np.array([2,4,6,8])
model = LinearRegression().fit(X, y)

print("coef:", model.coef_, "intercept:", model.intercept_)

#Stop Jupyter
In terminal:
Ctrl + C
y

Step 11 — Commit everything to Git
git add requirements-ml.txt ml_setup.ipynb
git commit -m "Add ML environment requirements and verification notebook"
git push origin main
